fname = input("Enter file name: ")

fh = open(fname)
count = 0

for line in fh:
    a=line.rstrip()
    b=a.split()
    if len(b)<3:
        continue
    if b[0] != 'From':
        continue
    count+=1
    print(b[1])

print("There were", count, "lines in the file with From as the first word")
